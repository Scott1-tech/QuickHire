{
  "name": "quickhire",
  "version": "1.0.0",
  "description": "CDL driver qualification portal with a company workdeck (invite drivers, collect applications in one place).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo \"SPA is prebuilt in public/app — no build step needed\"",
    "test:anna": "node anna/test/run.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^6.9.14"
  }
}
